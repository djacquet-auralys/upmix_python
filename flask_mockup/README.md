# Auralys Upmix Backend (Flask)

## Installation
```bash
python -m venv .venv
source .venv/bin/activate   # ou .venv\Scripts\activate sous Windows
pip install -r requirements.txt

python -m api.app
# écoute sur http://localhost:8000

